<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.cs407.around.PhotoReviewActivity"
    android:id="@+id/relativeLayout">

    <ImageView
        android:id="@+id/photo_review_imageview"
        android:background="@android:color/black"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="post"
        android:id="@+id/save_photo_button"
        android:background="@color/colorTransparent"
        android:textColor="@color/colorWhite" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="close"
        android:id="@+id/close_button"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:background="@color/colorTransparent"
        android:textColor="@color/colorWhite" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="add text"
        android:id="@+id/text_button"
        android:background="@color/colorTransparent"
        android:textColor="@color/colorWhite"
        android:layout_below="@+id/paint_button"
        android:layout_alignStart="@+id/paint_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="paint"
        android:id="@+id/paint_button"
        android:textColor="@color/colorWhite"
        android:background="@color/colorTransparent"
        android:layout_below="@+id/close_button"
        android:layout_alignStart="@+id/close_button" />

</RelativeLayout>
